rules_version = '2';

// Drive OAuth 백엔드 전용 데이터(driveUsers, driveSessions)는
// Admin SDK(Cloud Functions)로만 접근한다. 클라이언트 직접 접근은 전면 차단.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
